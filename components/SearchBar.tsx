/** @format */

"use client";
import React from "react";

function SearchBar() {
	const handleSearch = () => {};
	return (
		<form className='searchbar' onSubmit={handleSearch}>
			SearchBar
		</form>
	);
}

export default SearchBar;
